# IGNITE_ADDRESS defaults to localhost:10800 (standalone); set to node1:10800 in the docker sidecar.
ignite-client.addresses=${IGNITE_ADDRESS:localhost:10800}

spring.main.banner-mode=off
server.port=8080
